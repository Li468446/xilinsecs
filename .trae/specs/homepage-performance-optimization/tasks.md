# 首页加载速度优化任务列表

## 任务列表

### 任务1: 图片延迟加载优化
- [x] 1.1 为非首屏 `<img>` 标签添加 `loading="lazy"` 属性
- [x] 1.2 验证首屏图片正常加载，非首屏图片延迟加载

### 任务2: 视频加载优化
- [x] 2.1 将视频 `preload` 改为 `none`
- [x] 2.2 确保视频有 `poster` 属性作为预览图

### 任务3: 字体加载优化
- [x] 3.1 检查 Font Awesome 图标字体加载方式
- [x] 3.2 添加 `font-display: swap` 优化自定义字体

### 任务4: 验证与测试
- [x] 4.1 确认页面功能完整性
- [x] 4.2 确认所有文字内容完整保留

## 验收标准
- [x] 非首屏图片添加 `loading="lazy"`
- [x] 视频 `preload="none"` 且有 `poster`
- [x] 字体使用 `font-display: swap`
- [x] 页面所有文字内容完整保留
- [x] 页面功能正常运行
